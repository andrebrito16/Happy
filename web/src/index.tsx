import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

// método render recebe tags HTML (conteúdo JSX) e coloca isso dentro do elemento HTML, nesse ex root!
ReactDOM.render(
    <App />
  ,
  document.getElementById('root')
);

